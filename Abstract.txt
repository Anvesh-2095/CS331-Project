SOAR

We will need a simulation of alert.
One module will read them, normalise them and send them to other module
Other module will check playbook, and actuate those steps using some other module
in case if failure, it will pass message to other module which will record artifacts and notify human
